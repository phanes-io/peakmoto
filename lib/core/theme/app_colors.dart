import 'dart:ui';

class AppColors {
  AppColors._();

  // Backgrounds
  static const background = Color(0xFF1A1A1A);
  static const surface = Color(0xFF242424);
  static const surfaceHigh = Color(0xFF2E2E2E);

  // Accent – Amber
  static const amber = Color(0xFFFF9500);
  static const amberDark = Color(0xFFCC7700);
  static const amberGlow = Color(0x33FF9500);

  // Map
  static const routeLine = Color(0xFFFF9500);
  static const routeGlow = Color(0x55FF9500);
  static const curveHighlight = Color(0xFFFF4444);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFAAAAAA);
  static const textDisabled = Color(0xFF555555);

  // Status
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFFF4444);
}
